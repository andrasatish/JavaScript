/***
 * 01 (I). What is JavaScript?
 * 02 (I). variable in JS (VAR, LET, CONST)
 * 03 (I). Block level Scope (let) vs Functional Scope (var)
 * 04 (I). Callback functions or Higher order functions
 * 05. types (number, string, boolean, object, array, undefined, null)
 * 06 (I). == vs ====
 * 07 (I). Normal functions vs Arrow functions
 * 08 (I). How to check object is empty or not
 * 09 (I). How to retrieve keys and how to get length of the keys or object
 * 10 (I). How to retrieve values in object  and how to get length of the object
 * 11 (I). How to check whether is key is existed or not
 * 12 (I). Create (Object.assing(obj, {name:'sudheer'})), Update (using key and value), Delete (delete object key) the object properties.
 *     and add properties to existing object ((Object.assing(obj, {name:'sudheer'})), Dot (.) Notation, Square Notation). 
 * 13 (I). Dot Notation vs Square Notation
 * 14 (I). hasOwnProperty and destructuring 
 * 15 (I). loops (for, forEach (only works with arrays), for-of(arrays), for-in(objects))
 * 16 (I). conditions (if, if-else, else-if, switch, while, do-while)
 * 17. operator ( &&, ||, !==, !)
 * 18 (I). array
 *       [
 *            push,pop,shift,unshift,sort,reverse, splice -> original array changed
 *            indexOf, lastIndexOf, includes, find, findIndex, some, every -> element search and it will take callback function (except indexOf, lastIndexOf)
 *            map, filter, slice, reduce -> new array      
 *            join -> string
 *       ]
 * 19 (I). Check whether given characters existed or not (inlcudes);
 * 20 (I). Template string (`${variable_name}`)
 * 21 (I). Spread operator (...)
 * 22 (I). synchronous vs asynchronous (setTimeout, promise, async-await)
 * 23 (I). shallow copy vs deepcopy
 * 24 (I). self invoked functions or immediately invoked functions
 * 25 (I). function optional parameters[add(a?:number)], types [add(a:number)] and function default parameters[add(a:number = 20)] 
 * 26 (I). closures
 * 27 (I). call, apply and bind
 * 28 (I). primitive vs non-primitive
 * 29 (I). Hoisting
 * 30 (I). typed language
 * 31 (I). callback hell
 * 32 (I). callback vs promise
 * 33 (I). Event Propagation and Event Bubbling 
 */
