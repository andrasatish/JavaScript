let testData = 40;

//assign string value.
testData = 'sudheer';

//assign boolean value.
testData = true;

//assign array value.
testData = ['Banana','Apple'];

//assign Object value.
//OBJECT: Object is nothing but key and value pairs, Each key value pair seperate with colon(:). Each key and value pair we can call it property. Each property seprerate with comma(,).
testData = {
    name : 'sudheer',
    age : 28,
    location: 'hyderabad',
    name : 'satish', //last value will update,
    address: ['Mandapeta','Hyderabad'],
    employee: {
        name: 'sudheer',
        age: 28
    },
    isMarried: false,
}
console.log(testData); 